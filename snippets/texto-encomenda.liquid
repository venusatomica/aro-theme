{% for collection in product.collections %}
    {% if collection.handle == 'sob-encomenda' %}
        <div>
           <p> ::</P>
        </div>
        {% else %}
        <div>
            <p>.</p>
        </div>
    {% endif %}
{% endfor %}